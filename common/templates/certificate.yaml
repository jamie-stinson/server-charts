# {{- if and .Release.Name "cert-manager" .Values.certificateIssuer.enabled }}
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: {{ .Values.certificateIssuer.name }}-certificate
# spec:
#   secretName: {{ .Values.certificateIssuer.name }}-tls
#   issuerRef:
#     kind: ClusterIssuer
#     name: {{ .Values.certificateIssuer.name }}
#   dnsNames:
#     {{- range .Values.certificateIssuer.solvers.dns.dnsZones }}
#     - "{{ . }}"
#     {{- end }}
# {{- end }}